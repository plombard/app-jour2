# Configuration file
# key = value

greeting.message = BJR depuis le fichier
quarkus.log.file.enable=true
# Send output to a trace.log file under the /tmp directory
quarkus.log.file.path=/tmp/trace.log
